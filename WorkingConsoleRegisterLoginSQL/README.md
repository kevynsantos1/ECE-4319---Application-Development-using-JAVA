This is a console application where the user can not log in unless registered. When registering the user must provide a password and username that is then saved
into the SQL database. When the user tries to login the program checks the database to confirm the credentials. if credentials are correct the login is approved, 
otherwise it is not. Server can see these successful or fail logs.
